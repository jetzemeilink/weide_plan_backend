# find a way to deal with DateTime in graphql format
scalar DateTime

type Booking {
    id: ID!
    guest: Guest!
    spot: Spot!
    campingEquipment: CampingEquipment!
    invoice: Invoice!
    arrivalDate: DateTime
    departureDate: DateTime
    comment: String

}